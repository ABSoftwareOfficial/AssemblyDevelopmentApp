<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:AssemblyDevelopmentApp.Styles">

    <Style x:Key="BaseTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundColorBrush}" />
    </Style>

    <Style x:Key="FormLabel" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlock}">
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="2 0" />
    </Style>

    <Style x:Key="TitleTextBlock" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlock}">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="Margin" Value="1 5" />
    </Style>

    <Style x:Key="DescriptionTextBlock" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlock}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Margin" Value="2 5" />

        <Setter Property="Foreground" Value="{StaticResource DarkForegroundColorBrush}" />
    </Style>

    <Style x:Key="ErrorTextBlock" TargetType="TextBlock" BasedOn="{StaticResource DescriptionTextBlock}">
        <Setter Property="Foreground" Value="{StaticResource BadFormColorBrush}" />
    </Style>

</ResourceDictionary>